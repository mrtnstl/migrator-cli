# migrator-cli

CLI tool for centralized DB migration management.

Currently supporting `postgres` and `sqlite`, will add further drivers, as i need them.

#### (temporary, low res demo gif)

![migrator_demo](https://github.com/mrtnstl/migrator-cli/blob/main/docs/migrator_demo.gif "migrator_demo")

## Setup

1. clone the repo `git clone [this-repos-url]`
2. install dependencies with `npm ci`
3. build the project with `npm run build`
4. start the app with `npm start`
